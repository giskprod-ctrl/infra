// Subset of the signature-base ruleset maintained by Nextron Systems.
// Rules are curated to avoid duplicates and reduce false positives in the triage pipeline.

include "malware_tools.yar"
include "lateral_movement.yar"
